# 風哨 GALEPOST — 未完成事項紀錄

最後更新：2026-08-24（第十輪）

這份是「做到哪裡、還差什麼」的清單，接手的人（或下一次的我）直接照這份往下做。
分成三區：**卡在外部**（不是程式問題）、**功能待做**、**驗證待補**。

---

## 一、卡在外部，程式端已就緒

| 項目 | 狀態 | 要做什麼 |
|---|---|---|
| 後端免金鑰管線 | ✗ **檔案不在這個 repo** | `scripts/fetch_typhoon.py` 與 `.github/workflows/typhoon.yml` 在上一輪寫過，但沒有隨著程式碼一起帶過來，這個 repo 裡沒有。`data/typhoon.json` 目前是 `hasKey:false` 的空殼，前端會自動略過。要恢復免金鑰路徑，得先把抓取腳本補回來，再到 Settings → Secrets → Actions 設 `CWA_API_KEY`——**只設 Secret 不會有任何作用**。 |
| 中央氣象署實測回應 | ✗ 未對過 | 沙箱連不到 `opendata.cwa.gov.tw`，所有 CWA 解析都是拿仿造的回應測的。第一次接上真的金鑰時，要看一眼「官方警報卡」的內容是否與氣象署網站一致（特別是影響地區與發布時間）。 |
| CORS 代理 | △ 有退路但未實測 | 瀏覽器直連氣象署常被 CORS 擋。程式已經是「直連失敗 → 改走代理」，代理網址填在設定頁。實際擋不擋、代理格式對不對，要有真環境才知道。 |
| Docker 打包 | ✗ **檔案不在這個 repo** | `site/Dockerfile`、`docker-compose.yml` 同樣沒帶過來。不過站臺是純靜態檔，任何靜態伺服器都能服務，Docker 並非必要。 |

---

## 二、功能待做

原本列的六項**都做完了**（實測風速、侵襲機率、外推路徑、時間軸、
預報員解讀文字、風場對焦），另外加做了暴雨。

接下來可以考慮的（沒有人要求，只是想得到的）：

- **雷達回波圖層疊到自家地圖**：現在雷達要靠 Windy 的 iframe，
  疊進 Leaflet 才能跟站臺、路徑放在同一張圖上比對。
- **關機決策留痕**：把「當時看到什麼數據 → 做了什麼決定」一起存進紀錄，
  事後檢討時才知道當下的判斷依據，而不是只剩結果。
- **多單位／分區**：目前所有站臺是一份清單，若要跨單位使用需要分組。

## 三、這一輪已完成（2026-08-24 第十輪）

- **戰力分母不再納入未建置與自用監看**（要求 1、5）。原本寫的是
  `戰力 ${n.on} / ${DB.stations.length}` —— 分子已經排除了，分母卻是全部站數，
  所以 36/37 那個 37 裡面還有一個未建置的點。改成分母＝分子的同一組
  （已建置且會進交接通報的站），戰力條的格數也跟著一致。
- **兩指暫時放大**（要求 2）。`viewport` 鎖了 `maximum-scale=1`
  （為了不讓 iOS 在點輸入框時把整頁抽走），代價是連想把小字看清楚都沒辦法。
  自己接兩指手勢：撐開時以**兩指中點**為原點放大內容（最多 3 倍），
  手指離開就用 0.22s 的曲線彈回 —— 這是「湊近看一眼」，不是切換縮放層級。
  單指捲動完全不受影響；**地圖上不接管**（Leaflet 自己有雙指縮放）。
- **狀態顏色只給目前狀態**（要求 3）。兩個地方一起改：
  1. 站臺卡的三顆狀態鈕：原本「可按的」是彩色、「目前的」是灰的 ——
     等於三張卡上同時亮著紅與琥珀，看起來像三個警告而不是一個狀態。
     改成**目前狀態填色**（並維持不可按，它本來就已經是了），其餘中性但仍可按。
  2. 上方三格統計：數字為 0 就整格壓暗。紅色的「0」看起來像警報，
     其實它的意思是「一臺都沒關」。
- **字級移到設定最上面**（要求 4）。它是「看不清楚的時候第一個要找的東西」，
  原本埋在版本號旁邊等於找不到。拆成獨立區塊 `insertBefore` 到最前面。
- **中繼站臺（自用監看）預設不列入管制清單，要點該類別才顯示**（要求 5）。
  跟未建置同一個原則：管制盤是「現在有幾臺、狀態如何」。
  類別篩選鈕上加「＊」並用專屬色，否則會以為站臺不見了。
- **自用監看有自己的顏色並框起來**（要求 5）。新增 `--aux`（紫）。
  **刻意不沿用開機／待機／關機那三色** —— 那三色講的是管制狀態，
  這裡要講的是「這一臺不在交接名冊上」，是另一件事。
  整張卡用紫色外框、左側色條改紫、標籤從「不列入回報」改成更直白的「自用監看」。
- **狀態變更時序跟著清單的篩選走**（沒有要求，但不做就不一致）。
  上面看不到的站、下面時序裡卻冒出來，會讓人以為清單漏了誰。

---

## 四、上一輪已完成（2026-08-24 第九輪）

- **未建置站臺不列入站臺管制清單**（要求 1）。管制盤是「現在有幾臺、狀態如何」，
  還沒架上去的東西擺在裡面只會佔位置。改成預設不顯示，
  按「未建置」篩選鈕可檢視，設定頁的站臺清單仍然是完整的。
  統計那一行改成明講「N 處未建置（不在下方管制清單，按「未建置」可檢視）」。
- **全螢幕地圖**（要求 2）。18.75rem 高的框裡看不出誰是誰。加了 ⛶ 鈕，
  Esc 或再按一次離開。**切換之後不只 invalidateSize，還要重新框一次** ——
  不然全螢幕打開來，內容還擠在原本那個高度的位置，下面一大片空白。
- **官方雨量基準改成逐縣市**（要求 3，依使用者提供的第四條附表）。
  原本一律平地 350／山區 200 是錯的，附表其實是：
  - 多數縣市**不分山區平地一律 350**
  - 桃園、新竹縣、苗栗、臺中、南投、雲林、嘉義縣、高雄、臺東：**山區 200／平地 350**
  - **連江縣 200**（不分山區平地）
  - 三個縣另訂**「實際觀測」基準**（附表備註二寫明同為決定及通報依據）：
    **南投縣平地 300、宜蘭縣 250、連江縣 180**
  這一項改變了程式的判斷方式：附表的值是**未來 24 小時累積雨量預測**，
  拿雨量站的**實測**去比它本來只是參考 —— 但上面那三個縣**訂了實測基準**，
  所以那三個縣的實測達標是**官方依據**。畫面上把兩者分開標（達標時會標出門檻值，
  官方實測基準另標「（實際觀測）」）。
- **各縣市停班停課官方資訊**（要求 4）。分兩件事：
  - **官方基準**已內建（第四條附表），直接列出您的站臺與關注地點所在縣市的門檻值。
  - **官方即時公告**：**我沒有驗證過任何可跨網域取用的官方端點**（這個環境連不到
    dgpa.gov.tw，無從確認），所以**不內建猜測的網址** —— 那就是重演抓
    `W-C0034-003`（平台上不存在的資料集）那個錯，只是這次錯的東西會是「今天放不放假」。
    做法：預設給官方公告頁的連結；設定頁另開一個「公告 JSON 端點」欄位，
    您拿到可用的端點填進去，程式就會自動帶進颱風頁，並把您自己的縣市排到最前面標「我的」。
    取得失敗時明講失敗原因，不會靜靜顯示空白。

- **修掉一個原本就有的錯：`window.MAP` 永遠是 undefined**（沒有要求，是驗全螢幕時發現的）。
  `MAP` 是頂層 `let`，**頂層 `let` 不會掛到 `window` 上**，所以
  `if (window.MAP) …` 那兩段程式碼從來沒有執行過。影響：
  1. 全螢幕切換後不會重新框選（實測 zoom 停在 6，修好之後是 8）；
  2. **字級切換之後地圖從來沒有被通知重新量尺寸** —— 那段註解寫著「地圖容器尺寸
     跟著字級變，Leaflet 需要被告知重新量測」，但它一次也沒有做到。
  兩處都改成直接用 `MAP`，並在字級切換時一併重新框選。

---

## 五、更早（2026-08-24 第八輪）

上一輪末尾提的三項建議，使用者說「依照你的推薦做」，加上站臺清單排序。

- **雨量款接上了：用氣象署的豪大雨特報當替代訊號**。
  官方雨量款看的是「未來 24 小時累積雨量**預測**」，本程式沒有預測雨量 ——
  但**豪大雨特報本身就是預測性質**，而且分級門檻跟停班停課同一個口徑：
  超大豪雨 ≥ 500、**大豪雨 ≥ 350（＝平地基準）**、**豪雨 ≥ 200（＝山區基準）**。
  所以「氣象署對這個縣市發了大豪雨特報」＝「官方預測該地 24 小時雨量達 350 mm」，
  這比拿雨量站的**實測**累積去比 350 更貼近官方的判斷口徑。
  縣市怎麼來：站臺沒有縣市欄位，取最近雨量站（沒有就取最近氣象站）的縣市，
  兩者都限 60 km 內。對不上就**回 null 不判斷** —— 說錯縣市就是說錯放假。
  「臺中市山區」「南投縣山區」這種只套用到山區地形的點。臺／台混用先正規化再比對。
  仍然寫明這是**替代訊號不是官方數字**：特報是分級，不是逐點預測值。
- **中繼站臺在播報稿標出「不列入回報」**。使用者說的是「不需列入交接」，
  交接的是回報稿；播報是值班自己讀的氣象概況，把自己要看的站臺拿掉反而奇怪。
  改成留著但標出來，看的人才知道它為什麼不會出現在通報裡。
- **戰力與分頁角標不再把中繼站臺算進去**。三格數字要跟交接通報對得起來 ——
  通報上寫幾處、畫面上就該是幾處，差額沒有人解釋得出來。
  改成另外一行寫「另有 N 處不列入回報（自用監看）、M 處未建置，未計入上方戰力」。
- **站臺清單新增排序**（使用者要求）：**由北至南／名稱／類別／自訂順序**，預設由北至南。
  存成 `cfg.listSort`，**跟站臺頁的排序分開** —— 站臺頁的排序是給值班掃狀態用的，
  會用到風級與離颱風遠近；設定頁的清單是用來「找到某一站去改它」，要的只有位置與名字。
  關注地點清單套用同一個排序。清單每一列補上類別與「未建置」標示。
- **修掉兩句會誤導的話**（沒有要求，是驗收截圖時看到的）：
  1. 雨量款原本沿用風力款的「（推估自中午起）」。特報只是「這段期間這個縣市可能下到那個量」，
     拿它去推「幾點開始放」是把分級當成逐時預測，講得比手上的資料還確定。改成只寫日期。
  2. 公告時點會印出「昨日 20:00 前」這種已經過去的時間。改成過期時說
     「公告時點已過，如未公告會在今日 04:30 前補發」。

---

## 六、更早（2026-08-24 第七輪）

使用者接上真實金鑰之後回來的一輪。八項要求，做了七項＋兩項沒要求的。

- **未建置站臺**（要求 1）。新增 `plan` 狀態：不進回報稿、不算戰力、不做關機推估、
  不進衝擊預判與播報、不進停班停課推估 —— 那個點還不存在，替它算幾點該關機沒有意義。
  仍留在站臺清單與地圖上（虛線空心圖標），建置完成後自己改成開機即可。
- **播報稿精簡**（要求 2）。原本一站一句，十幾站就是十幾行，而且十幾行講的都是
  「推估不需關機」—— 要在那面文字裡自己找出真正會被影響的那幾站。
  改成：只列**會達到關機門檻**的站，其餘一行帶過（「其餘 N 站推估尖峰 6–7 級」），
  最後補一行**影響時段**（全區尖峰的時間範圍）。雨量同理，只列豪雨以上或已達停班課基準的。
  要逐站細節的話，「站臺」子頁籤本來就是完整的表。
- **播報稿上色**（要求 3）。上色的對象只有三種：會改變決定的**數字**（風級用蒲福色階、
  雨量等級用特報色、時刻用主色）、**動作**（應於…前完成關機、停班停課＝紅）、
  以及**但書**（※、注意事項＝整行壓暗）。其餘一律不動 —— 每個字都是重點就等於沒有重點。
  **是一行一行處理的**：整段一起 replace 的話，但書會先被前面的規則塗紅塗粗，
  再被但書規則包一層灰，內層贏，結果但書比正文還醒目。
  複製出去的仍然是純文字，不含任何標記（有常設斷言擋著）。
- **停班停課時段推估**（要求 4、5）。這是這一輪最要緊的一項，也是唯一一項我原本說「不做」
  的東西 —— 現在做得出來，是因為找到了**可以算的那一款**：
  《天然災害停止上班及上課作業辦法》的風力款是**平均風 7 級以上或陣風 10 級以上**，
  而逐時風場本來就有。掃一遍就得到達標時段，再換算成「哪一天、全日還是自中午起」。
  陣風以持續風的 1.3 倍估算。公告時點寫的是慣例（前一日 20:00 前，來不及者當日 04:30 前）。
  **雨量款算不出來**：它要的是未來 24 小時累積雨量的「預測」，本程式沒有預測雨量。
  卡片與播報稿都寫明：這是推估不是公告、還要「已致災或有致災之虞」、宣布權在地方政府。
- **關注地點**（要求 5）。住家、學校、家人所在地。跟站臺分開存 ——
  站臺問的是「設備幾點該關」，關注地點問的是「那裡幾點可能停班停課」。
  在設定頁新增，出現在停班停課推估卡、播報稿與地圖（青色 ★）。
- **地圖上的站臺**（要求 6）。**顏色講狀態**（開機綠／待機琥珀／關機紅／未建置虛線灰）、
  **符號講類別**（雷達 ◎、紅外線 🔺、中繼 📡，可在設備類別裡自己改成任何字或 emoji）。
  點圖標直接開該站的編輯。名稱標籤接到既有的疏化機制上 ——
  十幾個站臺擠在西海岸，縮小時標籤會疊成一團，疊在一起等於全部看不見。
- **圖層開關**（要求 7）。每個類別自成一個圖層，關注地點另一層，右上角可逐類開關。
  **併進原本的底圖控制項**，不另外開一個 —— 兩疊一模一樣的圖示，使用者要猜哪個是哪個。
- **地圖對焦切換**（沒有要求，但不做的話要求 6 等於白做）。原本永遠框「颱風＋站臺」，
  颱風還在一千多公里外時，站臺就是海岸線上的一小撮，
  「我的站臺現在長怎樣」在那個縮放下根本回答不了。加了「全域／對焦站臺」，選擇會記住。
- **圖例補上站臺那一列**（沒有要求）。顏色的意思不寫出來，紅點看起來像警告而不是「關機中」。

---

## 七、更早（2026-08-24 第六輪）

- **停班停課基準改成依站臺地形取一個**（使用者指出「雷達站都是平地，應該是 350 毫米」）。
  這是我上一輪做錯的地方：`dgpaMark` 同時比對 350 與 200，
  於是一個岸際雷達拿到 240 mm 會被標成「已達山區基準」——
  **那個站臺根本不適用山區基準**，標了就是假警訊。
  改成一個站臺只有一個門檻：`terrain` 記在**設備類別**上
  （岸際雷達／港區雷達站／紅外線熱影像一律 `flat` 350 mm），
  `dgpaMark(h24, st)` 只比那一個。
  **個別站臺可以覆寫**（站臺編輯裡的「地形」，預設「依類別」）——
  同一種設備架在山上還是海邊是位置的事，不是設備的事。
- **新增「中繼站臺」類別**（使用者要求）：地形山區（200 mm），
  並且**不列入回報稿**（使用者說「這是我自己看的，不需要列入交接」）。
  類別多了一個 `noReport` 旗標，回報稿的兩種樣板、已復機、待機四處全部排除。
  **仍然出現在站臺頁、衝擊預判與播報** —— 值班的人自己要看，只是不對外發。
  站臺卡上加一枚「不列入回報」標籤，否則看不出來為什麼它沒出現在通報裡。
  設備類別的編輯頁加了「地形」下拉與「列入回報稿」開關，兩者都可自行改。
  舊存檔沒有這兩個鍵，加了一次性遷移（`terrMigrated` 旗標）：
  缺 `terrain` 的一律當平地、缺 `noReport` 的當 false，
  並在沒有同名類別時補上「中繼站臺」。

  **沒有把中繼站臺從播報稿排除。** 使用者說的是「不需列入交接」，
  交接的東西是回報稿；播報是值班自己讀的氣象概況，把自己要看的站臺拿掉反而奇怪。
  若連播報也不要，這是一行的事。

---

## 八、更早（2026-08-24 第五輪）

- **「站台」全部改成「站臺」**（使用者要求）。程式、離線快取、manifest、文件共 119 處。
  順手把 `台灣` 一律改成 `臺灣` —— 原本畫面上「距臺約 300 公里」與「台灣東南東方」
  兩種寫法混著出現，看起來像打錯字。
  回報稿的解析器**沒有**寫死這兩個字（區塊標題是 `HDR_RE` 泛用比對），
  所以舊的回報文字照樣貼得進來、解析得出來。
- **站臺依地點由北至南排序**（使用者要求）。新增 `ns` 排序（緯度遞減），
  **設為預設**，沒填座標的排在最後 —— 不知道在哪裡就不能假裝知道它在北邊還是南邊。
  **回報稿的每個區塊也一併改成由北至南**（原本是照狀態變更時間排；
  通報是給人照地理位置對照的，時間先後在紙上看不出任何意義）。
  「關機在待機之前」的分組順序不變，衝擊表也維持依風級高到低 —— 那是分級表，不是名冊。
  舊使用者的存檔裡是 `custom`，加了一次性遷移（留 `nsMigrated` 旗標），
  否則已經裝過的人永遠看不到這個改動。**自訂順序沒有被破壞** ——
  它存在 `DB.stations` 的陣列順序裡，切回「自訂順序」原本的排法就回來了；
  使用者自己選過之後不會再被搬走（已用「選 custom → 重新載入」驗證）。

- **修掉一個原本就有的嚴重錯誤：舊存檔會把新設定的預設值弄成 `undefined`。**
  `DB = Object.assign(base, o)` 會**就地改掉** `base`，於是下一行的
  `Object.assign(base.cfg, o.cfg)` 變成拿存檔那份蓋自己，
  存檔裡沒有的鍵全部留在 `undefined`。
  影響：每次新增設定（`seaBuffer`、`bfEmerg`、`lockMin`…），
  對已經裝過的人都是 `undefined`，而 **`風級 >= undefined` 恆為 false** ——
  關機門檻會整個失效，畫面上卻什麼都看不出來。`DB.ty` 有同一個問題。
  改成先把預設值抓在手上，再 `Object.assign({}, 預設, 存檔)`。
  是在驗排序時發現回報稿最後一行印出「五、undefined」才追出來的。
  回歸測試加入常設檢查：**餵一份只有 `unit` 與 `bfShut` 的舊存檔，
  自己有的值不能被蓋掉、缺的鍵必須補回預設**。

---

## 九、更早（2026-08-24 第四輪）

- **停班停課雨量參考基準**（依使用者提供的兩張截圖推斷需求：人事行政總處
  「天然災害停止上班及上課情形」公告頁 + 雨量基準）。
  **刻意做成「參考基準」而不是「判斷」** —— 兩者差在三個地方，
  少講任何一個都會讓值班的人把它當成放假預告：
  1. 官方基準看的是**未來 24 小時累積雨量「預測」**，程式手上只有雨量站的**實測**累積；
  2. 光達到雨量還不夠，要件是「**且已致災或有致災之虞**」；
  3. 宣布權在**地方政府綜合研判**，達標不必然停班停課。
  實作：`DGPA_HILL = 200`、`DGPA_FLAT = 350`（毫米／24 小時），
  站臺卡與衝擊表的雨量欄在達標時多一行「停班課已達山區／平地基準」，
  播報稿在有任一站達標時附上完整基準與上述三項但書，
  並在設定頁與實況頁放上人事行政總處公告連結（`dgpa.gov.tw/typh/daily/nds.html`）。
  **沒有做的**：即時逐縣市停班停課狀態。人事行政總處沒有已驗證、可跨網域取用的
  資料端點，這個環境也連不出去驗證；憑猜測接一個端點就是重演
  `W-C0034-003`（抓一支根本不存在的資料集）那個錯。所以只給連結與基準。
- 順手修掉雨量那一行的折行：「大／豪雨」被拆成兩行看起來像兩個等級，
  等級與停班課標記各自加 `white-space:nowrap`。

---

## 十、更早（2026-08-22 第三輪）

- **修掉「熱帶性低氣壓也推算颱風警報」的錯誤**（使用者發現）。兩個 bug 疊在一起：
  1. 氣象署沒給 `Circle15ms` 時，`parseCyclone` 會依強度**憑空生一個 150 公里**的
     七級風半徑。但熱帶性低氣壓**根本沒有暴風圈** —— 近中心最大風不到 8 級，
     哪來的七級風範圍。
  2. `predictWarnings` 只看 `r15 >= 距離`，不看那個系統是不是颱風。
     於是那個假的 150 公里罩住 96 公里外的臺灣 → 「暴風範圍影響中」→
     推算出海警與陸警。
  中央氣象署的颱風警報**只發給近中心最大風達 8 級（17.2 m/s）以上的颱風**；
  低於此的系統是改用豪大雨特報、強風特報因應。
  修法：低於門檻時 `r15 = 0`（不無中生有）；`predictWarnings` 逐時檢查
  「當下是否已達颱風強度」（低壓期間增強成颱風的情況仍算得到）；
  警報卡改成明講為什麼不會有警報，只留「中心最接近」；
  地圖不再畫不存在的暴風圈；主卡七級風半徑顯示「—」。
  回歸測試加入常設檢查：**未達颱風強度時不得出現颱風警報推算**。

- **播報稿改成分段格式**（使用者提供範本）。從一段一段的長句改成
  【今日氣象概況與警報】標頭 + 分節（颱風動態／颱風警報／降雨特報分級／
  站臺衝擊／雨量實測／注意事項），每節用全形空格縮排，可直接貼進通報。
- **時間表達規範**（使用者指定）：當天＝今日、隔天＝明日、再隔天＝後天、
  第四天起用星期（週四、週五），並加上時段（凌晨／清晨／上午／中午／午後／
  傍晚／晚間／深夜）。「今日晚間 22:00」比「8/24 22:00」少一次心算。
  超過一週改回日期 —— 講「週三」會分不出是哪一個週三。
  同一套講法也套到**警報時機卡**（原本是「8/27 01:00 · 3.8 天後」，
  現在主要顯示「週三凌晨 01:00」）與**時間軸**。
  衝擊表維持精簡格式 —— 表格是用掃的，不是用唸的。
- **修掉播報稿的一句矛盾**：改寫時我讓它在「推估不需關機」後面
  又接「可評估復機」。`stationImpact` 在沒超過門檻時會把 `resume` 算成「現在」，
  所以那句話既矛盾又是假的。改成只有真的需要關機時才講復機。

- **臺灣近海 100 公里界線**（使用者要求）。海上颱風警報的門檻就是這條線
  （暴風範圍可能在 24 小時內進入臺灣、澎湖、金門、馬祖近海 100 公里以內海域）。
  畫在地圖上，暴風圈還差多遠碰到門檻一眼就看得出來。
  **關鍵：直接拿 `distToOutline` 這個函式本身反解等值線**，而不是另外描一條
  「看起來差不多」的線 —— 畫在圖上的必須就是警報時機真正用的那條，
  否則圖說一套、算出來的時間又是另一套。實測界線上每一點的誤差為 0 km，
  設定改成 50 km 也照樣吻合。
- **NCDR 天氣與氣候監測網連結**（使用者要求）。放在實況頁與設定頁。
  那裡可以比對各國預報路徑（氣象署／美軍／日本／韓國／中國）與衛星雲圖。
- **地圖圖例**。顏色變多之後（紅=選中、琥珀=其他颱風、青=預報路徑、
  淡青虛線=近海門檻），不講一下沒人知道那條虛線是什麼。
- **資料過期警示**（沒人要求，但我認為該做）。這支程式是快取優先、
  颱風天訊號又常斷，畫面上明明有數字、其實可能是一小時前的。
  超過 30 分鐘就明講「資料已是 N 分鐘前取得的」，超過 90 分鐘轉成紅色。
  過期的颱風資訊比沒有資訊更危險 —— 這本來就是這支程式寫在 sw.js 裡的原則。

- **氣象署特報移到播報頁籤**。它是寫通報時要抄的材料，跟播報稿擺一起才順手，
  也不會把颱風卡撐長 —— 「大雨」一次可以列到十六個縣市。
  版面改成：等級標籤固定在左邊（等寬對齊），地區在右邊自己換行，有效時間放在地區下方。
- **修掉回報頁的日期框超出視窗**。`box-sizing` 是全域設好的，問題出在
  iOS Safari 的 `datetime-local` 有自己的內在最小寬度，`width:100%` 壓不過它。
  加上 `min-width:0` 才讓它可以縮，`appearance:none` 去掉原生外框。
- **站臺卡改用狀態詞而非動作詞**。原本寫「8月22日 1132時 完成開機」，
  在還沒有颱風的時候看起來像剛執行過復機作業，其實只是一直開著。
  改成「開機中　8月22日 1132時 起」。`S_VERB`（完成開機／實施待機／完成關機）
  留給回報稿用 —— 那裡本來就是在講動作。

- **颱風強度分級**（使用者要求）。畫面上原本只有蒲福風級（14 級），
  但通報寫的是「中颱」。加上氣象署的強度分級：
  熱帶性低壓（< 17.2 m/s）／輕颱（17.2–32.6）／中颱（32.7–50.9）／強颱（≥ 51.0）。
  主卡寫「中度颱風　近中心最大風 14 級」，選擇列寫「中颱 14 級 · 677 km」，播報稿一併帶上。
- **修掉特報抓不到影響地區**：`walkAdvisory` 只把 `locationName` 往下傳承，
  但 `W-C0033-002` 的影響地區在 `info.affectedAreas.location[]` ——
  也就是在 `phenomena` 的**下面**而不是上面。記到 phenomena 那一刻地區還是空的，
  所以卡片上三張特報全部沒有影響地區。改成碰到 phenomena 時先掃自己的子樹。
- **特報併進颱風卡**：原本三張獨立卡、每張掛一次「中央氣象署發布」，
  使用者反映看不懂那個徽章是什麼。改成颱風卡底部一條「氣象署特報」，
  等級做成彩色小標籤 + 影響地區 + 有效時間，徽章拿掉。
- **子頁籤重排**：概況 → 播報 → 路徑 → 實況 → 站臺。
- 刪掉播報下方「可直接複製貼進通報。官方數字與本機推估已分開標示…」那句；
  播報稿裡三句「中央氣象署發布 X 特報」合成一句。

- **颱風頁改成子頁籤**（使用者要求）。原本整頁攤開要滑很久，
  現在分成 **概況／路徑／站臺／播報／實況** 五個分頁，
  每頁高度從約 4000 px 降到 900–1350 px。選擇列常駐在頁籤外
  （切颱風會影響每一個分頁），選過的分頁會記住。
  地圖切回來時會重新量尺寸再重新框 —— 隱藏狀態下量不到自己的大小。
- **贅詞刪除**：三張特報卡（每張都掛一次「中央氣象署發布」）合成一張，
  徽章只出現一次；資料來源那段從兩句話縮成一行；
  警報時機、實測、雨量的說明各砍掉一半以上。保留的是安全性警語，
  砍掉的是重複與客套。
- **無名稱的颱風改用氣象署編號**（TD22、TD23）。三顆都叫「未命名」分不出誰是誰。
- **選擇列名稱不再被截斷**（「沙…」）。名稱獨佔一行，級數與距離移到第二行。
- **修掉回報名稱不同步**：從 GDACS 換到氣象署資料之後，名字還停在 `NARRA-26`，
  主卡上跟實際那顆對不起來。改成「使用者沒自己改過就跟著選中的颱風走」，
  並把主卡／地圖／播報改成顯示氣象署那顆颱風的真名（回報欄位仍可自訂）。
- **時刻優先於時距**：預報表第一欄從「+6h」改成「8/23 08:00」（時距縮小放第二行），
  時間軸也把真實時刻放最大 —— 排班要的是幾點，不必自己加減。
- 主卡座標改成 `21.0°N 128.0°E`；原本的度分格式在三分之一寬的格子裡尾巴的 E 會被切掉。

- **地圖呈現改成 ARGUS 的樣子**（使用者指定）：
  會旋轉的 🌀 颱風符號（`divIcon`）+ **常駐的名稱／時間標籤**。
  四顆颱風同圖時靠 tooltip 認不出來 —— 要點一下才看得到等於看不到。
  選中的用紅色、其餘用琥珀色（和面板上的選擇列對得起來），點次要颱風即切換。
  預報點標上「時刻 + 時距」，外推路徑的標籤另外標「外推」。
  次要颱風**納入框選範圍**，多颱時一眼看得到全部幾顆在哪裡。
  尊重系統的「減少動態」設定 —— 會轉的東西在值班畫面上很吵。
- **標籤疏化**：預報點標籤縮小時會擠成一團，疊在一起等於全部看不見。
  依螢幕距離疏化，颱風名稱標籤永遠優先保留，放大之後其餘自然回來。
  （實測 zoom 4 顯示 2/6、zoom 7 顯示 6/6、zoom 2 顯示 2/6。）
- **修掉一個既有的地圖 bug**：`fitBounds` 排在 `invalidateSize` 之前，
  剛從 `display:none` 切過來時容器還沒有尺寸，Leaflet 拿 0×0 去算縮放，
  結果縮到 **zoom 0（整個世界）**，颱風與站臺全部擠在畫面中央一點。
  改成先 `invalidateSize` 再 `fitBounds`，並過濾掉非有限的座標。

- **找到「一直看不到颱風」的真正原因**（比對使用者另一支程式 ARGUS 的原始碼）。
  中央氣象署回的座標是 **`CoordinateLatitude` / `CoordinateLongitude` 兩個分開的欄位**，
  不是 `"lng,lat"` 字串。`parseLL` 只認字串，拿到 `undefined` 就整顆回 `null`，
  於是每一顆颱風都被 `filter(Boolean)` 丟掉 → 空陣列 →
  畫面顯示「中央氣象署目前沒有發布中的颱風路徑資料」。
  **從頭到尾都不是氣象署沒資料，是解析錯欄位。**
  一併修好的其他欄位：
  - 觀測時刻是 `DateTime`，不是 `fixTime`
  - 暴風半徑是 `Circle15ms.Radius`，**而且可能是陣列**（各象限分開給），取最大值
  - 預報半徑是 `Radius70PercentProbability`（沒有 `Of`）
  - 預報時距 `Tau` / `ForecastHour`，缺了就用 `DateTime` 減 `ForecastData.InitialTime`
  - 只有一顆颱風時 CWA 直接給物件而不是長度 1 的陣列（加了 `toArr`）
  - 中心氣壓是 `PressureCenter`
- **天氣特報改用遞迴走訪**（同樣參考 ARGUS）。特報結構在不同資料集之間不一致，
  照固定形狀解析很容易整份落空；改成沿途記住最近的 `locationName` 與 `endTime`，
  碰到 `phenomena` 就記一筆。資料集順序改為 `W-C0033-002` →`-003`→`-001`
  （`-002` 欄位最完整，也是 ARGUS 實際在用的那支）。

- **對照氣象署實際的資料集清單，修掉兩個打錯的**（依使用者提供的 Swagger 截圖）：
  - `W-C0034-003`（颱風侵襲機率）**平台根本沒有這支**。天氣警特報底下只有
    `W-C0033-001`~`005` 與 `W-C0034-001`、`W-C0034-005`。已整段移除 ——
    抓一個不存在的資料集，只會每次更新多送一個必然失敗的請求。
  - 豪大雨特報是 **`W-C0033-003`**，不是我原本打的 `-001`
    （`-001` 是「各縣市目前之天氣警特報」）。改成 `-003` 優先，
    取不到再依序退到 `-002`、`-001`。
- **版本顯示與手動更新**。這是使用者「看不到新功能」的真正原因：
  程式本體是快取優先，裝過一次之後手機上可能一直是舊版，**而畫面上看不出來**。
  設定頁現在會印出版本號與 service worker 狀態，並有「檢查更新」按鈕。
  已實測：安裝 v6 → 按檢查更新回報「已是最新」→ 站臺部署 v7 → 再按 →
  換成 v7 且舊快取清掉。仍然維持「不自動抽換」的原則（颱風天不該把正在操作的人抽換掉）。

- **風場對焦**（原 TODO 第六項）。Windy 嵌入新增「對焦站臺／對焦颱風」切換，
  切到颱風會以颱風中心為中心並拉遠一級（看得到整個環流），選擇會記住。
  沒有颱風時不顯示這個切換 —— 切過去只會是一片空海。
  **預設仍是站臺**：值班最常問的是「自家設備頭上在下什麼」。
  順手處理 TODO 提醒的流量問題：Windy 圖磚刻意不快取，重掛一次 iframe
  就是又一輪流量，所以網址沒變就不重載（重複點同一個按鈕不再重新載入）。

- **可拖曳的時間軸**（原 TODO 第四項）。逐時內插序列本來就算好了，只差介面。
  滑桿拖到哪一小時，就顯示該時刻的颱風位置、強度、距臺灣距離，
  以及**各站臺在那個時刻是幾級風**（依風級排序）。地圖上同步出現該時刻的
  白色虛線暴風圈與中心標記，拖回「現在」就撤掉，免得跟主標記重疊。
  外推路徑時標題會標「（外推推估）」。
  修掉一個順手發現的圖層洩漏：時刻標記是獨立圖層，主圖重畫時清不到它，
  只把變數設成 null 會讓舊圖層留在地圖上，每重畫一次多疊一個。

- **暴雨**（新增，非原 TODO）。兩條線一起做：
  `O-A0002-001` 自動雨量站給「已經下了多少」，`W-C0033-001` 天氣特報給
  「氣象署認為接下來會下多少」。衝擊表新增「雨量」欄（24 小時累積 + 時雨），
  依氣象署標準分級（大雨／豪雨／大豪雨／超大豪雨）。豪大雨特報做成卡片放在颱風頁上方，
  同一種特報的縣市會合併，**過期的特報會濾掉**（擺著會讓人以為還生效），
  非雨類特報（強風之類）不收 —— 風已經有颱風警報單在管。
  **關機門檻仍只看風**，雨量是給值班的人研判聯外道路與積水風險用的。
- **氣象員播報**（原 TODO 第四項「預報員解讀文字」）。把距離、方位、強度、
  侵襲機率、官方警報、豪大雨特報、最接近時間、各站臺推估與實測、雨量，
  串成一段可以直接唸、直接抄進通報的話，附「複製」鈕。
  **官方數字與本機推估逐句分開標示**，實測與推估差三級以上會單獨寫一句警告。
  移動方向會把 WNW 這類縮寫轉成中文，播報稿唸得出來。

- **官方侵襲機率**（原 TODO 第二項）。`W-C0034-003`，放在颱風頁最上面，
  依機率高低排序取前 6 個縣市，標成「中央氣象署官方數字」的綠色徽章 ——
  這是整頁唯一一組官方預測數字，其餘全是本機推估，不能混在一起看。
  **對不上就不顯示**：編號 → 英文名 → 中文名三種都比不中就空著。
  刻意沒有「只有一筆就直接用」的退路 —— 測試時就是那條退路把
  另一顆颱風的 99% 掛到了這一顆頭上。掛錯比空白危險。

- **外推路徑**（原 TODO 第三項）。官方只給分析位置、沒有預報路徑時（常見於熱帶性低氣壓），
  用移動向量直線外推 24 小時，警報時機與站臺衝擊因此算得出來。
  **移速低於 8 km/h 不外推**——太慢的系統方向沒有意義。
  有官方預報時**絕不**外推。標示照抄 TODO 的要求：段落標題寫「外推路徑（非官方預報）」、
  上方一張橘色警告寫明假設的方向與速度、表格每一列標「外推」、
  地圖改用橘色疏虛線、警報時機卡也標「由外推路徑推算」。強度不外推（沿用現值並說明）。

- **實測風速接上了**（原 TODO 第一項）。`O-A0001-001` 自動氣象站，
  取離每個站臺最近、且 60 公里內的測站，把**實測**與**推估**在衝擊表裡並排。
  兩者差 3 級以上會標「差 N 級」——那代表模型在該地形下不可信，
  值班的人要以現場為準，不能照程式給的時間關機。站臺卡上也一併顯示。
  觀測值只留在記憶體、不寫進本機儲存：過期的風速比沒有風速更危險。
  氣象署的 -99 之類哨兵值會濾掉，不會被當成「風速 0」。
  新舊兩種 API 形狀都接（`Station`/`WeatherElement` 物件 與 `location`/`weatherElement` 陣列）。
- **「沒有颱風」不再含糊**。`fetchCWAAll` 以前在「氣象署真的沒發布」與
  「氣象署有給、但我們每一顆都解析失敗」兩種情況都回傳空陣列，顯示同一句話。
  現在會比對「收到幾筆 / 解析出幾筆」，後者會明講**這是程式的問題，不是氣象署沒發布**。
- **說明文字不再被重畫吃掉**。原本 note 是逐次傳進 `renderTyphoon` 的，
  切個頁籤重畫就沒了，只剩「尚未取得颱風資料」。現在留到下次載入才換。

---

## 十一、更早（2026-08-22 第二輪）

- **站臺上線**：https://xin7355-collab.github.io/GALEPOST/
  （2026-08-22 部署成功。前兩次失敗是 Actions 權杖開不了 Pages，
  由 repo 擁有者在 Settings → Pages 把 Source 設成 GitHub Actions 之後就通了。
  以後推到 `claude/feng-shao-lgn3wn` 會自動重新部署。）

- **Leaflet 收進 repo**（`vendor/leaflet/`，1.9.4，取自 npm 官方套件）。
  以前從 unpkg 載，斷線時地圖整個不見；現在 service worker 一併快取，
  離線重新整理仍然開得出地圖框架與控制項（圖磚本身還是要連網）。
  順帶不再為了一張底圖把使用者的瀏覽紀錄送去第三方 CDN。
- **修掉一個會把錯誤訊息吃掉的競態**：路徑（`W-C0034-005`）與警報單（`W-C0034-001`）
  是同時發出的兩個請求，卻共用同一個 `_cwaDiag` 變數。警報單成功時會把
  「路徑抓不到」的原因擦掉，畫面又變回看起來像「氣象署沒發布」——
  正好是這段診斷本來要防的那件事。改成依資料集分開存放，已用測試重現並確認修好。
- **Pages 部署流程**（`.github/workflows/pages.yml`）。
- 補回 `manifest.webmanifest`、`assets/`、`data/typhoon.json`（空殼）。

---

## 十二、更早（2026-08-22 第一輪）

- **雙颱、三颱全部顯示**：`fetchCWA` 原本只取 `list[0]`，等於把第一顆以外的颱風靜靜丟掉——
  同一把授權碼、同一份資料，風哨只看得到一顆而別的程式看得到四顆，原因就在這一行。
  現在全部解析、依距臺灣由近而遠排序，畫面上給選擇列，點了整份推算（內插、警報時機、
  站臺衝擊、回報稿的颱風名稱）全部跟著換。重新更新資料時會記得使用者選的是哪一顆。
- **地圖改成彩色**：預設彩色衛星影像（Esri World Imagery，與 ARGUS 同一套），
  另外提供彩色街道圖與原本的暗色圖，切換後會記住。路徑線加了深色襯線，
  在亮處也看得見。底圖來源標示依授權要求打開。
- 後端 `fetch_typhoon.py` 一併輸出颱風識別碼，讓「選中的那顆」在兩條資料路徑上都能對上。

---

## 十三、驗證現況

已經在無頭瀏覽器測過並通過的：

- 版面：**直式 / 橫式 / 平板橫** × **小 / 中 / 大 / 特大** 共 12 組，含三顆颱風的選擇列，無溢出、無逐字換行。
- 三顆颱風解析、排序、切換、重新更新後選擇不跳掉。
- 底圖預設衛星影像、三種可切換、切換後記住。
- 官方警報卡、座標解析、回報產出、字級切換、PWA 安裝與離線，皆為既有測試，這次一併重跑通過。

這一輪補測的：

- 三顆颱風（法拉／哈爾西／고란）解析、依距臺灣排序、選擇列切換，整份推算跟著換，無錯誤。
- 地圖**首次真的畫出來**（Leaflet 收進來之後沙箱也載得動）：圖層切換、縮放、來源標示、
  路徑線與站臺標記都在。
- Service worker 快取 12 項全中，舊的 v4 快取清掉，離線重新整理後 Leaflet 仍在。
- 競態修正：路徑失敗 + 警報單成功（後回）時，失敗原因不再被擦掉。

沒辦法在這個環境測的：真實 CWA 回應、真實地圖圖磚畫面、Docker、部署後的網址。

這一輪（第四輪）補測的：

- 回歸矩陣擴成 **4 種資料情境（雙颱 PascalCase／單颱物件／熱帶性低壓／完全無資料）
  × 3 種視窗（360／320／768）× 2 種字級（中／特大）= 24 組**，
  每組走完颱風頁五個子頁籤與三個主頁籤，共 213 項斷言全數通過：
  無橫向溢出、無 JS 例外。
- 停班停課門檻的常設斷言：380 mm → 平地基準、240 mm → 山區基準、
  60 mm 與無資料 → 不標記；站臺頁與播報稿實際渲染出該標記；
  播報稿必須同時出現「預測」與「地方政府」（缺任一就是把參考講成判斷）。
- 熱帶性低壓的常設斷言再加嚴：`r15` 必須為 0、`WARN.isTy === false`、
  海警與陸警皆為 null、畫面不得出現「海上颱風警報」字樣。
- `APP_VER` 與 `sw.js` 的 `CACHE` 版本一致（版本不同步 = 使用者永遠拿到舊版）。

這一輪（第五輪）補測的：

- 用字：颱風／站臺／回報／設定四頁的畫面文字、播報稿、回報稿，
  **一律不得出現「站台」或「台灣」**，回報稿必須出現「站臺」。
- 排序：陣列順序刻意排成「南→北→中→無座標」，且變更時間與緯度反向 ——
  避免「照時間排剛好也對」的假通過。驗排序函式輸出、**畫面卡片的實際順序**、
  無座標排最後、回報稿內的出現位置，以及「選 custom → 重新載入後仍是 custom」。
- 舊存檔補預設：`bfEmerg`／`bfResume`／`seaBuffer`／`humMax`／`leadMin` 必須是有限數，
  回報樣板與類別清單必須補回，`DB.ty` 六個鍵必須齊全。
- 全部 239 項斷言通過（含前四輪的 24 組版面矩陣）。

這一輪（第六輪）補測的：

- 地形門檻：測資刻意放一個**平地類別、卻落在 240 mm 雨量站旁**的站臺
  （「中海拔雷達」），斷言它**不得**出現停班課標記 —— 這正是上一輪的錯。
  另驗中繼站臺 240 mm 要標、199 mm 不標、個別站臺覆寫成山區後 240 mm 要標，
  以及未指定站臺時退回平地。
- 不列入回報：回報稿 A、B 兩種樣板都不得出現中繼站臺的站名或區塊標題，
  但**站臺頁與衝擊預判必須看得到它**，且站臺頁要出現「不列入回報」標籤。
- 舊存檔遷移後類別數為 4（原三類 + 中繼站臺）。
- 全部 257 項斷言通過。

這一輪（第七輪）補測的：

- 未建置：回報稿 A／B 都不得出現、`impactMap` 不得含它、播報與衝擊預判不得含它，
  但站臺頁必須看得到並有專屬提示。
- 停班停課：風力款門檻為 7／10 級；平均風 7 級達標、6 級不達標；
  概況頁必須出現推估卡且卡上必須有「推估／公告為準／地方政府」的字樣。
- 播報精簡：**「推估不需關機」出現次數必須為 0**（原本一站一次），
  且必須有「影響時段：」那一行。
- 上色：畫面必須有 `<span style="color:`，而 `briefText()` 的純文字**不得**含任何標記；
  「超大豪雨」不得被「大豪雨」從中間切開。
- 地圖：圖層依類別建立、關注地點自成一層、**圖層控制項全域只有一個**、
  未建置用虛線圖標、對焦切換有顯示；**連續重畫 4 次，圖標數與控制項數都不變**
  （時刻標記那個圖層洩漏的坑，這次先寫測試擋著）。
- 全部 283 項斷言通過。

這一輪（第八輪）補測的：

- 雨量款：新增一組資料情境（新北市大豪雨、南投縣山區豪雨）。
  斷言大豪雨 → 平地站達標（350 ≥ 350）、山區豪雨 → 中繼站達標（200 ≥ 200）、
  **「南投縣山區」特報不得套用到同一個雨量站旁但屬平地類別的站**、
  沒有特報的縣市回 null 不判斷；推估卡與播報稿都要出現「雨量款」，
  且播報稿必須載明特報是替代訊號。
- 戰力：中繼站臺不得計入三格，站臺頁必須說明另有幾處不列入回報。
- 清單排序：選項必須含 `ns` 與 `name`；預設由北至南（驗實際 DOM 順序）；
  切成名稱後順序必須改變且符合 `localeCompare(zh-Hant)`；選擇必須存進 `cfg.listSort`。
- 資料情境增為 5 種（雙颱／單颱／低壓／豪大雨特報／無資料），版面矩陣 30 組。
- 全部 344 項斷言通過。

## 還沒解決、需要真實環境確認的

**使用者實機截圖上的疑點**：颱風最接近時距臺 7 公里（等於登陸），
但所有站臺尖峰只有 6–7 級、全部「推估不需關機」。
一顆 15 級的中颱在 7 公里外，西海岸站臺只有 7 級，這個數字偏低。
可能是模型沒有地形、站臺全在西岸；也可能是渦旋衰減參數不適用這個距離。
**沙箱裡驗不了**，需要真的接近時比對現場實測。在那之前，
畫面上「推估不需關機」這句話不能當成可以不關機的依據。

這一輪（第九輪）補測的：

- 第四條附表：22 個通報權責機關全部入表；基隆不分山區平地皆 350、
  臺中山區 200／平地 350、臺／台混用查得到、連江 200 與實際觀測 180、
  宜蘭 350 與實際觀測 250、**南投實際觀測 300 只適用平地（山區必須是 null）**、
  附表未列的縣市回退到預設。
- 實測標記：宜蘭 260 mm 要標且 `official === true`；宜蘭 240 不標；
  **新北 260 mm 不得標**（新北沒有實際觀測基準，260 未達預測值 350）；
  新北 360 標但 `official === false`；連江 190 標為官方。
- 官方公告卡：未設來源時必須明講「不代抓即時公告」、必須有官方連結、
  `DGPA_LIVE` 必須是 null（**不得有假資料**）、附表門檻必須列在卡上。
- 全螢幕：預設不是全螢幕；按鈕後填滿視窗且**zoom 必須變大**（重新框選）；
  全螢幕時站臺圖標仍在；Esc 離開後高度還原且 body overflow 復原。
- 未建置：預設管制清單不得出現、統計行要指出、按「未建置」篩選後要看得到。
- 全部 370 項斷言通過。

這一輪（第十輪）補測的：

- 戰力：分母必須等於「已建置且會進通報」的站數，且**不得等於全部站數**；
  戰力條的格數必須等於分母。
- 統計格：數字為 0 的格必須有 `zero`、非 0 的必須沒有。
- 狀態鈕：一張卡上**只有一顆**是停用（＝上色）的。
- 兩指放大：合成兩點 `touchstart`／`touchmove` 事件，
  驗證加上 `peeking`、`transform` 為 `scale(2)`（距離 100→200）、
  放開後 `transform` 清空且 `peek` 類別全部移除。
- 字級：必須是 `#cfgBody` 的**第一張卡**，且 `#tsSeg` 全域只有一個
  （renderSettings 被包了一層，重複附加會出現兩份）。
- 自用監看：預設清單不得出現、點該類別後要出現、卡片要有 `.st.aux` 外框、
  標籤要是「自用監看」。
- 全部 382 項斷言通過。
